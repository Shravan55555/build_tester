env:
    #MANIFEST: https://github.com/AxionAOSP/android.git -b lineage-23.0 --git-lfs
    #LOCAL_MANIFEST: https://github.com/shravansayz/local_manifests.git -b axion

task:
  name: "build"
  timeout_in: 40m
  container:
      image: shazuxdd/ubuntu:latest
      cpu: 8
      memory: 32G
  Storage-Checker_script:
      - ./scripts/d0wnl0ad
  Sync_script:
      - ./scripts/syn0
  upload_script:
      - ./scripts/upl0ad
